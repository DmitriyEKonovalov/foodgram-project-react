version: '3.3'

services:
  db:
    image: postgres:13.0-alpine
    volumes:
      - /var/lib/postgresql/data/
    env_file:
      - ./.env

  # исходный код
  # frontend:
  #   build:
  #     context: ../frontend
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ../frontend/:/app/result_build/

  web:
    image: dmitriykonovalov/infra-backend:latest
    restart: always
    volumes:
      - static_value:/app/backend/static/
      - media_value:/app/backend/media/
    depends_on:
      - db
    # env_file:
    #   - ./.env

  frontend:
    image: dmitriykonovalov/infra_frontend:latest
    restart: always
    volumes:
      - ../frontend/:/app/result_build/
    depends_on:
      - web
    env_file:
      - ./.env

  nginx:
    image: nginx:1.19.3
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/:/usr/share/nginx/html/api/docs/
    depends_on:
      - web
      - frontend

volumes:
  static_value:
  media_value:
  postgres_data:
